DATABASE_URL="postgresql://user:pass@localhost:5432/dentalos?schema=public"
PORT=3000

# Secret for signing/verifying auth JWTs (use a long random value in prod)
AUTH_JWT_SECRET="change-me-in-production"
